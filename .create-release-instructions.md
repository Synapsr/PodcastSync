# Instructions pour créer la release v0.1.0 sur GitHub

Vous avez **2 options** pour créer la release avec les builds macOS et Windows :

---

## 🤖 OPTION A : Automatique avec GitHub Actions (Recommandé)

**Avantages** : Builds automatiques pour macOS (Intel + Apple Silicon) ET Windows

### Étapes :

1. **Activez GitHub Actions** :
   ```bash
   git add .github/workflows/release.yml
   git commit -m "ci: Add GitHub Actions workflow for multi-platform releases"
   git push
   ```

2. **Supprimez et recréez le tag v0.1.0** :
   ```bash
   git tag -d v0.1.0
   git push origin :refs/tags/v0.1.0
   git tag -a v0.1.0 -m "Initial release v0.1.0"
   git push origin v0.1.0
   ```

3. **Attendez le build** (10-15 min) :
   - Surveillez : https://github.com/Synapsr/PodcastSync/actions
   - Une fois terminé, allez sur : https://github.com/Synapsr/PodcastSync/releases

4. **Finalisez la release** :
   - Cliquez sur "Edit" sur la release draft
   - Remplacez la description par le contenu de `.github-release-notes.md`
   - Cliquez sur "Publish release"

**Résultat** : Vous aurez automatiquement :
- ✅ macOS Intel (x64.dmg)
- ✅ macOS Apple Silicon (aarch64.dmg)
- ✅ Windows (x64-setup.exe et .msi)

📖 Guide détaillé : `.github-actions-setup.md`

---

## 👤 OPTION B : Manuelle (Plus rapide mais macOS uniquement)

**Avantages** : Release immédiate, simple

### Étapes :

1. **Allez sur GitHub Releases** :
   ```
   https://github.com/Synapsr/PodcastSync/releases/new
   ```

2. **Configurez la release** :
   - **Tag** : `v0.1.0`
   - **Title** : `v0.1.0 - Version Initiale / Initial Release`
   - **Description** : Copiez `.github-release-notes.md`

3. **Uploadez le binaire macOS** :
   - Glissez-déposez : `src-tauri/target/release/bundle/dmg/PodcastSync_0.1.0_aarch64.dmg`

4. **Options** :
   - ✅ "Set as the latest release"
   - ⬜ PAS "pre-release"

5. **Publiez** :
   - Cliquez sur "Publish release"

**Résultat** : Release avec macOS Apple Silicon uniquement
- ⚠️ Windows et macOS Intel devront attendre v0.1.1 avec GitHub Actions

---

## 💡 Recommandation

**Option A (GitHub Actions)** est fortement recommandée car :
- Tous les utilisateurs (Windows + macOS) pourront télécharger
- Automatisation pour les futures releases
- Gratuit pour les repos publics

Si vous avez besoin d'une release **immédiatement**, faites l'Option B maintenant, puis configurez GitHub Actions pour v0.1.1.

---

## 📝 Notes

- Le système de mise à jour automatique fonctionne dès la publication
- Pour supprimer les fichiers temporaires après :
  ```bash
  rm .github-release-notes.md .create-release-instructions.md .windows-build-instructions.md .github-actions-setup.md
  ```
